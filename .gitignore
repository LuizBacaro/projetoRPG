# Variáveis de ambiente — NUNCA commitar
.env

# Banco local
*.db

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.venv-ci/
.venv_pdf/
venv/
*.egg-info/

# Uploads locais
uploads/
backend/uploads/
backend/uploads/**

# Materiais de referência locais (PDFs, planilhas, scripts de extração)
livros/
helpers/

*.pdf

# IDE
.vscode/
.idea/

# Extrações locais de PDF (pdftotext); não versionar trechos grandes
docs/_extracted/

# Scripts: artefatos gerados em runtime (não versionar)
backend/scripts/generated/

# Assets binários gerados: manter apenas CSVs de referência, não JSONs grandes
backend/scripts/assets/*.json
backend/scripts/assets/*.xlsx

# Backups e dumps de banco — nunca versionar (podem conter credenciais)
*.dump
*.sql.gz
*.backup
*_backup.sql
*_backup.dump
*_producao_*.json
neon_*.dump

# Scripts temporários de sessão
tmp_*.py
tmp_*.sh

# Temp de validacao e runners
/tmp/*.copilot.*

# Scripts de extração PDF (artefatos de teste)
extract_*.py
find_*.py

# Arquivos acidentais gerados em tempo de edição (caracters especiais, espaços, etc)
"(Mending, Ler Magias)"
".py', 'r') as f:"
"= all_text.split('n')"
"orted([m[0] for m in"
pdf:
e:
"tra primeiras"