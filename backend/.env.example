# Copie este arquivo para .env e preencha os valores
# cp .env.example .env

# CRÍTICO: gere uma chave aleatória
# python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=TROQUE-POR-UMA-CHAVE-SECRETA-ALEATORIA

# Dev local
DATABASE_URL=sqlite:///./rpg_arena.db

# Produção Railway (Railway injeta automaticamente)
# DATABASE_URL=postgresql://user:pass@host:port/db

# CORS (produção)
# ALLOWED_ORIGINS=https://seu-dominio.com.br